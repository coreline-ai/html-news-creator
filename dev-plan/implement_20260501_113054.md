# 2026-05-01 — 10개 섹션 생성 안정화 및 이미지 fallback 개선

## 목표
- Full pipeline이 5분 제한으로 generate 단계 전에 종료되어 이전 6개 보고서가 계속 보이는 문제를 줄인다.
- 이미지가 없는 후보를 backfill 단계에서 탈락시키지 않고, 생성형 대표 카드 SVG로 대체한다.
- 실제 생성 결과에서 target 대비 부족분과 생성 이미지 fallback 수를 추적 가능하게 만든다.

## 원인 요약
- `app/admin/run_runner.py` 기본 타임아웃이 300초라 `collect/extract/classify/cluster/verify`만으로도 종료될 수 있다.
- `data/editorial_policy.yaml` 및 기본 policy의 `backfill_require_image: true`가 이미지 없는 후보의 backfill 진입을 막는다.
- `scripts/run_daily.py`에는 이미 `create_section_fallback_svg()`가 있지만, selection에서 탈락하면 fallback 생성 기회가 없다.

## 구현 항목
- [x] 기본 Run 타임아웃을 900초로 확대하고 설정값으로 분리
- [x] 기본 backfill 정책을 “이미지 필수”에서 “이미지 선호 + 없으면 생성 fallback”으로 변경
- [x] Policy/NewReport UI 문구를 source image requirement로 명확화
- [x] generate 통계에 target/shortfall/generated fallback 수 추가
- [x] selection 단위 테스트 갱신
- [x] 관련 README 문구 갱신

## 검증 계획
- [x] selection unit test
- [x] per-run option mapping test
- [x] UI typecheck/test/build
- [ ] 실제 full run 재실행은 사용자 승인 후 별도 진행
